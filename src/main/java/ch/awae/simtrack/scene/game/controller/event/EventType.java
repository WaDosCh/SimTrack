package ch.awae.simtrack.scene.game.controller.event;

public enum EventType {

	/**
	 * The currently selected tile has changed. As first parameter contains: TileCoordinate
	 */
	SELECTION_CHANGED;
}
